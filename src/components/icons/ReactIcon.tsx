import { SiReact } from "@icons-pack/react-simple-icons";
import type { ReactElement } from "react";

export default function ReactIcon(): ReactElement {
    return <SiReact color="#61DAFB" height={20} />;
}
