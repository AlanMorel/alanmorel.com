import { SiBetterauth } from "@icons-pack/react-simple-icons";
import type { ReactElement } from "react";

export default function BetterAuthIcon(): ReactElement {
    return <SiBetterauth color="#000000" height={20} />;
}
