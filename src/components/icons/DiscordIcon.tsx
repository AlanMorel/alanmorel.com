import { SiDiscord } from "@icons-pack/react-simple-icons";
import type { ReactElement } from "react";

export default function DiscordIcon(): ReactElement {
    return <SiDiscord color="#848eff" height={20} />;
}
